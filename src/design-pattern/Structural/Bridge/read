桥接模式：将抽象和行为刬分开来，能各自独立，但能能态结合。
在面向对象设计的基本概念中，对象这个概念实际上是由属性和行为两部分组成的，属性我们可以认为是一种静止的，是一种抽象，一般情况下，行为是包含在一个
对象中的。但是，这种情况下，我们也需要将这种行为归类，形成一个总的接口，这就是桥接模式的一个好处。
为什么使用？
如果一个抽象类或接口有多个具体实现（子类，concrete subclass）,1）这些子类是并列的（方形柱，圆形柱）2）这些子类概念重叠
例如:中杯加奶、大杯加奶、 中杯不加奶、大杯不加奶,从分离抽象和行为的角度，使用Bridge模式来实现。

